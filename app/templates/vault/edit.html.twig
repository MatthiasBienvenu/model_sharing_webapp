{% extends 'base.html.twig' %}

{% block title %}Edit Vault{% endblock %}

{% block body %}
    <h1>Edit Vault</h1>

    {{ include('vault/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_vault_index') }}">back to list</a>

    {{ include('vault/_delete_form.html.twig') }}
{% endblock %}
